parameters:
  tasks:
    phpunit:
      always_execute: true
    phpcs:
      standard: ./phpcs.xml
      ignore_patterns: [./vendor/*, ./app/*]
    phpstan:
      level: 0
      ignore_patterns: [./vendor/*, ./app/*]
      triggered_by: ['php']
